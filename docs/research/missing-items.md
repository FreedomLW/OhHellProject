# Missing / follow-up items

Potential gaps to address next:
1. Align runtime implementation with the updated `RULES.md` (36-card deck, fixed diamond trump, and low-card-count bidding exception).
2. Define strict policy for when docs are moved from `planning` -> `wip` -> `done` (manual vs automated).
3. Add architecture decision records (ADRs) for major future game-rule or RL-environment changes.
4. Add lint/type-check standards to complement tests in CI.
5. Implement the agent redesign plan in `docs/planning/agent-redesign-plan.md` (bruteforce tactical agent, random bid-target curriculum, staged rewards, and robust tournament evaluation protocol).
