# Harness adoption plan

## Objective
Adopt a consistent engineering harness for OhHellProject docs and verification.

## Planned tasks
- [x] Add `AGENT.md` with project context and task workflow.
- [x] Add `RULES.md` describing current game rules.
- [x] Add `ARCHITECTURE.md` describing system design.
- [x] Establish documentation lifecycle directories under `docs/`.
- [x] Add CI workflow to run tests and ensure required docs exist.
- [x] Mark potentially missing/ambiguous items explicitly.

## Completion criteria
- All files are present and committed.
- Test suite passes in CI-compatible command (`pytest`).
